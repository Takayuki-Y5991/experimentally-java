components:
  schemas:
    # Domain exceptions
    ApiErrorResponse:
      type: object
      properties:
        status:
          type: integer
          example: 400
          description: HTTP Status Code
        message:
          type: String
          example: Invalid Parameter, etc...
          description: Message = Invalid Parameter, Invalid Access, etc ...
    